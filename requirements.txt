# ==============================================================================
# trading_bot dependencies
# ==============================================================================

# python-binance: Unofficial python wrapper for the Binance Spot and Futures Exchange API
python-binance>=1.0.19

# python-dotenv: Reads key-value pairs from a .env file and sets them as environment variables
python-dotenv>=1.0.0

# rich: Writing rich text (with color and style) to the terminal, tables, panels, and prompts
rich>=13.0.0
